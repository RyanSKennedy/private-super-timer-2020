//
//  Timer.swift
//  SuperTimer2020
//
//  Created by Ryan S.Kennedy on 21.06.2020.
//  Copyright © 2020 Ryan S.Kennedy. All rights reserved.
//
import SwiftUI

/*struct Timers: Identifiable {
    var id = UUID()
    var name: String
    var headline: String
    var image: Image
}

let myData = [
    Timers(
        name: "Standart Stopwatcher",
        headline: "Default stopwatcher",
        image: Image(systemName: "play.circle.fill")
            /*.resizable()
            .aspectRatio(contentMode: .fit)
            .frame(width: 50, height: 50)
            .foregroundColor(.blue) as! Image*/
    ),
    Timers(
        name: "Standart Timer",
        headline: "Default timer",
        image: Image(systemName: "play.circle.fill")
            /*.resizable()
            .aspectRatio(contentMode: .fit)
            .frame(width: 50, height: 50)
            .foregroundColor(.red) as! Image*/
    ),
    Timers(
        name: "Advanced Timer",
        headline: "Timer available for modify",
        image: Image(systemName: "play.circle.fill")
            /*.resizable()
            .aspectRatio(contentMode: .fit)
            .frame(width: 50, height: 50)
            .foregroundColor(.red) as! Image*/
    ),
    Timers(
        name: "Interval Timer",
        headline: "Simple interval timer",
        image: Image(systemName: "play.circle.fill")
            /*.resizable()
            .aspectRatio(contentMode: .fit)
            .frame(width: 50, height: 50)
            .foregroundColor(.red) as! Image*/
    ),
    Timers(
        name: "Adv Interval Timer",
        headline: "Interval timer available for modify",
        image: Image(systemName: "play.circle.fill")
            /*.resizable()
            .aspectRatio(contentMode: .fit)
            .frame(width: 50, height: 50)
            .foregroundColor(.red) as! Image*/
    ),
    Timers(
        name: "Super Custom Timer",
        headline: "Maximum flexible timer",
        image: Image(systemName: "play.circle.fill")
            /*.resizable()
            .aspectRatio(contentMode: .fit)
            .frame(width: 50, height: 50)
            .foregroundColor(.red) as! Image*/
    ),
    Timers(
        name: "My Saved Timers",
        headline: "My presets of timers",
        image: Image(systemName: "play.circle.fill")
            /*.resizable()
            .aspectRatio(contentMode: .fit)
            .frame(width: 50, height: 50)
            .foregroundColor(.red) as! Image*/
    ),
]*/

struct Timers_Previews: PreviewProvider {
    static var previews: some View {
        /*@START_MENU_TOKEN@*//*@START_MENU_TOKEN@*/Text("Hello, World!")/*@END_MENU_TOKEN@*//*@END_MENU_TOKEN@*/
    }
}

